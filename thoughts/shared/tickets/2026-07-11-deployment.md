for deployment, what happened if i wanted to deploy postgres in 1 vps and our app and stuff in another vps? we will presumably need 2 docker compose no? maybe we need to create an infrastructure folder that has nginx and stuff in front of our app, then how we can connect between vps as well if i were to deploy the postgres only and the app in another vps. then we also need something like a makefile to make it easier for me. make deploy-db, make deploy-app. then put like different note on the env that i need to modify within the docker compose as well so i can remember later because after deploying postgres i need to update the env for the app before deploying as well right